version: '3.3'
services:
  db:
    image: postgres:13.10
    env_file: .env
    volumes:
      - pg_data:/vat/lib/postgresql/data
    ports:
      - "5434:5432"

  # backend:
  #     build: 
  #       context: ../bookshop
  #       dockerfile: Dockerfile
  #     env_file: ./.env
  #     volumes:
  #       - static:/backend_static
  #       - media:/app/media/
  #     depends_on:
  #       - db

volumes:
  # static:
  # media:
  pg_data:
  # redoc: